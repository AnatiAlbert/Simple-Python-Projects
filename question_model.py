# create class called question
class Question:

    # define init method with two parameters
    def __init__(self, q_text, q_answer):
        # define two attributes for text and answer
        self.text = q_text
        self.answer = q_answer
